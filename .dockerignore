# Git
.git
.gitignore

# Node / build artifacts
node_modules
.next
dist
build

# Runtime caches (regenerated at runtime — never needed in the image)
sniper_klines_cache.json
bybit_klines_cache.json
funding_cache.json
bybit_funding_cache.json
*_cache.json
*.cache.json

# Env / secrets
.env
.env.*
!.env.example

# Misc
*.log
.DS_Store
